---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: overleaf-https-ingress
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`{{ .Values.ingress.host }}`)
      services:
        - name: overleaf-service
          port: 80
  tls:
    secretName: overleaf-tls
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: overleaf-https-redirect
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: overleaf-http-ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`{{ .Values.ingress.host }}`) && !PathPrefix(`/.well-known/acme-challenge/`)
      middlewares:
        - name: overleaf-https-redirect
      services:
        - name: overleaf-service
          port: 80
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: overleaf-tls
spec:
  secretName: overleaf-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
